# Prisma CLI setup
# This file helps with local development of Prisma migrations

# Generate the Prisma client after changes to schema
# Run: npm run prisma:generate

# Push schema changes to database (development)
# Run: npm run prisma:push

# Create a new migration (with named migrations)
# Run: npm run prisma:migrate -- --name your_migration_name

# Open Prisma Studio (GUI for database)
# Run: npm run prisma:studio
